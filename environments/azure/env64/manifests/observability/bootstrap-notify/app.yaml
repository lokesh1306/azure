apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bootstrap-notify
  namespace: env64
  labels:
    app: bootstrap-notify
    environment: env64
    argocd.argoproj.io/agent-id: env64
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  destination:
    namespace: stackend
    name: env64
  project: default
  source:
    repoURL: stackai.azurecr.io
    chart: charts/bootstrap-notify
    targetRevision: 0.1.0
    helm:
      releaseName: bootstrap-notify
      valuesObject:
        environmentName: "env64"
        endpoints:
          - "https://env64.stack.ai"
          - "https://api.env64.stack.ai"
        loginUrl: "https://env64.stack.ai"
        supabaseDashboardUrl: "https://studio.env64.stack.ai"
        toEmails:
          - "lchallagulla@stack-ai.com"
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
    syncOptions:
      - CreateNamespace=false
