apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bootstrap-notify
  namespace: env62
  labels:
    app: bootstrap-notify
    environment: env62
    argocd.argoproj.io/agent-id: env62
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  destination:
    namespace: stackend
    name: env62
  project: default
  source:
    repoURL: stackai.azurecr.io
    chart: charts/bootstrap-notify
    targetRevision: 0.1.0
    helm:
      releaseName: bootstrap-notify
      valuesObject:
        environmentName: "env62"
        endpoints:
          - "https://env62.stack.ai"
          - "https://api.env62.stack.ai"
        loginUrl: "https://env62.stack.ai"
        supabaseDashboardUrl: "https://studio.env62.stack.ai"
        toEmails:
          - "lchallagulla@stack-ai.com"
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
    syncOptions:
      - CreateNamespace=false
