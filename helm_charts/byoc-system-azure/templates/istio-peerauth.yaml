{{- if .Values.istioPeerAuth.enabled }}
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-system
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  mtls:
    mode: {{ .Values.istioPeerAuth.mode }}
{{- end }}
